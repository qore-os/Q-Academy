# Q-Academy host egress policy, schema 1.
# The parser treats every non-comment line as data and rejects unknown keys.
policy_version=1
network.egress=tcp:80,tcp:443
network.proxy=tcp:80,tcp:443,udp:443
